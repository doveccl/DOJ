{
  "name": "doj",
  "version": "3.0.0",
  "description": "Online Judge for OI & ICPC",
  "repository": {
    "type": "git",
    "url": "https://github.com/doveccl/DOJ.git"
  },
  "author": "Doveccl <doveccl@live.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "tslint": "tslint -p tsconfig.json -c tslint.json server/**/*.ts",
    "dev:server": "nodemon --watch server/**/* -e ts --exec 'ts-node server'",
    "dev:web": "",
    "build:server": "tsc",
    "build:web": "",
    "clean": "rm -rf build dist",
    "build": "npm run clean && npm run build:server && npm run build:web"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "gridfs-stream": "^1.1.1",
    "js-yaml": "^3.12.0",
    "jszip": "^3.1.5",
    "koa": "^2.5.1",
    "log4js": "^2.8.0",
    "mongoose": "^5.1.3",
    "nodemailer": "^4.6.5"
  },
  "devDependencies": {
    "@types/koa": "^2.0.46",
    "nodemon": "^1.17.5",
    "ts-node": "^6.0.5",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  },
  "optionalDependencies": {
    "dcrypto": "^1.1.0",
    "mysql": "^2.15.0"
  }
}
