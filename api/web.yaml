openapi: 3.1.0
info:
  title: DOJ Web API
  version: 0.1.0
paths:
  /api/health:
    get:
      operationId: health
      responses:
        "200":
          description: Server health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
  /api/ready:
    get:
      operationId: ready
      responses:
        "200":
          description: Server dependencies are ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
        "503":
          description: Server dependencies are not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
  /api/events:
    get:
      operationId: watchEvents
      responses:
        "200":
          description: Server-sent events for lightweight live updates
          content:
            text/event-stream:
              schema:
                type: string
  /api/site:
    get:
      operationId: getSite
      responses:
        "200":
          description: Public site settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Site"
  /api/me:
    get:
      operationId: getMe
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Me"
    patch:
      operationId: updateMe
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeUpdate"
      responses:
        "200":
          description: Updated current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Me"
  /api/me/password:
    patch:
      operationId: updatePassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordUpdate"
      responses:
        "204":
          description: Password updated
  /api/auth/login:
    post:
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Signed in user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Me"
  /api/auth/register:
    post:
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Registered user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Me"
  /api/auth/logout:
    post:
      operationId: logout
      responses:
        "204":
          description: Signed out
  /api/languages:
    get:
      operationId: getLangs
      responses:
        "200":
          description: Submit languages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Lang"
  /api/uploads/images:
    post:
      operationId: uploadImage
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Uploaded image
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadResult"
  /api/admin:
    get:
      operationId: getAdmin
      responses:
        "200":
          description: Admin overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/settings:
    patch:
      operationId: updateAdminSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminSettings"
      responses:
        "200":
          description: Updated admin settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSettings"
  /api/admin/users:
    post:
      operationId: createAdminUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUserCreate"
      responses:
        "201":
          description: Created user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/users/{name}:
    patch:
      operationId: updateAdminUser
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUserUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
    delete:
      operationId: deleteAdminUser
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Deleted user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/users/{name}/password:
    post:
      operationId: resetAdminUserPassword
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Reset user password
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordReset"
  /api/admin/groups:
    post:
      operationId: createAdminGroup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminGroupUpdate"
      responses:
        "201":
          description: Created group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/groups/{id}:
    patch:
      operationId: updateAdminGroup
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminGroupUpdate"
      responses:
        "200":
          description: Updated group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
    delete:
      operationId: deleteAdminGroup
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/languages/{id}:
    patch:
      operationId: updateAdminLang
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminLangUpdate"
      responses:
        "200":
          description: Updated language
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
    delete:
      operationId: deleteAdminLang
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Deleted language
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/languages:
    post:
      operationId: createAdminLang
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminLangCreate"
      responses:
        "201":
          description: Created language
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/judgers/{id}:
    patch:
      operationId: updateAdminJudger
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminJudgerUpdate"
      responses:
        "200":
          description: Updated judger
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
    delete:
      operationId: deleteAdminJudger
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted judger
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/judgers:
    post:
      operationId: createAdminJudger
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminJudgerCreate"
      responses:
        "201":
          description: Created judger
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminOverview"
  /api/admin/backups/settings:
    get:
      operationId: getBackupSettings
      responses:
        "200":
          description: Backup settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupSettings"
    patch:
      operationId: updateBackupSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BackupSettings"
      responses:
        "200":
          description: Updated backup settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupSettings"
  /api/admin/backups:
    get:
      operationId: getBackups
      responses:
        "200":
          description: Backup list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupList"
    post:
      operationId: createBackup
      responses:
        "201":
          description: Created backup
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupItem"
  /api/admin/backups/{name}/download:
    get:
      operationId: downloadBackup
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Backup file
          content:
            application/gzip:
              schema:
                type: string
                format: binary
  /api/admin/backups/{name}:
    delete:
      operationId: deleteBackup
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted backup
  /api/home:
    get:
      operationId: home
      responses:
        "200":
          description: Home data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Home"
  /api/home/notice:
    patch:
      operationId: updateNotice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoticeUpdate"
      responses:
        "200":
          description: Updated notice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Home"
  /api/users/{id}/{year}/{month}/{day}/{name}:
    get:
      operationId: userMedia
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: year
          in: path
          required: true
          schema:
            type: string
        - name: month
          in: path
          required: true
          schema:
            type: string
        - name: day
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User uploaded media
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
  /api/problems:
    get:
      operationId: listProblems
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: tag
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Problem list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Problem"
    post:
      operationId: createProblem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProblemCreate"
      responses:
        "201":
          description: Problem created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /api/problems/{id}:
    get:
      operationId: getProblem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Problem detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Problem not found
    patch:
      operationId: updateProblem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProblemUpdate"
      responses:
        "200":
          description: Problem updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Problem not found
    delete:
      operationId: deleteProblem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Problem deleted
  /api/problems/{id}/visibility:
    patch:
      operationId: updateProblemVisibility
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProblemVisibilityUpdate"
      responses:
        "200":
          description: Problem visibility updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Problem not found
  /api/problems/{id}/assets:
    get:
      operationId: getProblemAssets
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Problem assets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemAssets"
  /api/problems/{id}/assets/images:
    post:
      operationId: uploadProblemImage
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Problem image uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadResult"
  /api/problems/{id}/assets/{name}:
    get:
      operationId: problemPublicAsset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Public problem asset
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
  /api/problems/{id}/data/{name}:
    get:
      operationId: problemPrivateData
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Private problem data file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
  /api/problems/{id}/judge/{name}:
    get:
      operationId: problemPrivateJudge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Private problem judge file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
  /api/problems/{id}/assets/files:
    post:
      operationId: uploadProblemAsset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [section, file]
              properties:
                section:
                  type: string
                  enum: [data, judge, assets]
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Problem asset uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemAssets"
    delete:
      operationId: deleteProblemAsset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: key
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Problem asset deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemAssets"
  /api/problems/{id}/assets/files/content:
    get:
      operationId: getProblemAssetContent
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: key
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Text asset content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetContent"
    patch:
      operationId: updateProblemAssetContent
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetContentUpdate"
      responses:
        "200":
          description: Text asset updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemAssets"
  /api/problems/{id}/assets/cases:
    post:
      operationId: createProblemCase
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetCaseCreate"
      responses:
        "201":
          description: Problem case created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemAssets"
  /api/problems/{id}/assets/template:
    post:
      operationId: fillJudgeTemplate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Judge template written
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemAssets"
  /api/problems/{id}.zip:
    get:
      operationId: downloadProblemAssets
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Problem assets zip
          content:
            application/zip:
              schema:
                type: string
                format: binary
  /api/assignments:
    get:
      operationId: listAssignments
      responses:
        "200":
          description: Assignment list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Assignment"
    post:
      operationId: createAssignment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignmentCreate"
      responses:
        "201":
          description: Assignment created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assignment"
  /api/assignments/{id}:
    get:
      operationId: getAssignment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Assignment detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignmentDetail"
        "404":
          description: Assignment not found
    patch:
      operationId: updateAssignment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignmentUpdate"
      responses:
        "200":
          description: Assignment updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assignment"
        "404":
          description: Assignment not found
    delete:
      operationId: deleteAssignment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Assignment deleted
  /api/contests:
    get:
      operationId: listContests
      responses:
        "200":
          description: Contest list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Contest"
    post:
      operationId: createContest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContestCreate"
      responses:
        "201":
          description: Contest created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contest"
  /api/contests/{id}:
    get:
      operationId: getContest
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Contest detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContestDetail"
        "404":
          description: Contest not found
    patch:
      operationId: updateContest
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContestUpdate"
      responses:
        "200":
          description: Contest updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contest"
        "404":
          description: Contest not found
    delete:
      operationId: deleteContest
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Contest deleted
  /api/submissions:
    get:
      operationId: listSubmissions
      parameters:
        - name: problem
          in: query
          schema:
            type: string
        - name: user
          in: query
          schema:
            type: string
        - name: assignment
          in: query
          schema:
            type: string
        - name: contest
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Submission list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Submission"
    post:
      operationId: createSubmission
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitRequest"
      responses:
        "201":
          description: Submission created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Submission"
  /api/submissions/{id}:
    get:
      operationId: getSubmission
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submission detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubmissionDetail"
        "404":
          description: Submission not found
    patch:
      operationId: updateSubmission
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmissionUpdate"
      responses:
        "200":
          description: Submission updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Submission"
        "403":
          description: Owner or admin required
        "404":
          description: Submission not found
  /api/rank:
    get:
      operationId: getRank
      responses:
        "200":
          description: Rank list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RankUser"
  /api/users/{name}:
    get:
      operationId: getUser
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "404":
          description: User not found
  /api/discussion:
    get:
      operationId: listDiscussions
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: tags
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Discussion list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Discussion"
    post:
      operationId: createDiscussion
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscussionCreate"
      responses:
        "201":
          description: Discussion created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Discussion"
  /api/discussion/{id}:
    get:
      operationId: getDiscussion
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Discussion detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscussionDetail"
        "404":
          description: Discussion not found
    patch:
      operationId: updateDiscussion
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscussionUpdate"
      responses:
        "200":
          description: Discussion updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Discussion"
        "404":
          description: Discussion not found
    delete:
      operationId: deleteDiscussion
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Discussion deleted
  /api/discussion/{id}/comments:
    post:
      operationId: createComment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommentCreate"
      responses:
        "201":
          description: Comment created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comment"
components:
  schemas:
    Health:
      type: object
      required: [status]
      properties:
        status:
          type: string
    Site:
      type: object
      required: [siteName, allowRegistration, allowGuestAccess, defaultSubmissionPublic]
      properties:
        siteName:
          type: string
          maxLength: 64
        allowRegistration:
          type: boolean
        allowGuestAccess:
          type: boolean
        defaultSubmissionPublic:
          type: boolean
    HeatCell:
      type: object
      required: [date, count]
      properties:
        date:
          type: string
        count:
          type: integer
    Item:
      type: object
      required: [id, title, meta]
      properties:
        id:
          type: integer
        title:
          type: string
          maxLength: 256
        meta:
          type: string
    Me:
      type: object
      required: [id, name, mail, bio, avatar, admin]
      properties:
        id:
          type: integer
        name:
          type: string
          maxLength: 32
        mail:
          type: string
          format: email
          maxLength: 256
        bio:
          type: string
          maxLength: 256
        avatar:
          type: string
          maxLength: 512
        admin:
          type: boolean
    MeUpdate:
      type: object
      required: [mail, bio, avatar]
      properties:
        mail:
          type: string
          maxLength: 256
        bio:
          type: string
          maxLength: 256
        avatar:
          type: string
          maxLength: 512
    LoginRequest:
      type: object
      required: [name, password]
      properties:
        name:
          type: string
          maxLength: 256
        password:
          type: string
    RegisterRequest:
      type: object
      required: [name, mail, password]
      properties:
        name:
          type: string
          minLength: 3
          maxLength: 32
        mail:
          type: string
          format: email
          maxLength: 256
        password:
          type: string
          minLength: 8
    Lang:
      type: object
      required: [id, name, source]
      properties:
        id:
          type: string
          maxLength: 32
        name:
          type: string
          maxLength: 64
        source:
          type: string
          maxLength: 128
    UploadResult:
      type: object
      required: [url]
      properties:
        url:
          type: string
    PasswordUpdate:
      type: object
      required: [oldPassword, newPassword]
      properties:
        oldPassword:
          type: string
        newPassword:
          type: string
    AdminOverview:
      type: object
      required: [settings, users, groups, languages, judgers, queue]
      properties:
        settings:
          $ref: "#/components/schemas/AdminSettings"
        users:
          type: array
          items:
            $ref: "#/components/schemas/AdminUser"
        groups:
          type: array
          items:
            $ref: "#/components/schemas/AdminGroup"
        languages:
          type: array
          items:
            $ref: "#/components/schemas/AdminLang"
        judgers:
          type: array
          items:
            $ref: "#/components/schemas/AdminJudger"
        queue:
          $ref: "#/components/schemas/AdminJudgeQueue"
    AdminSettings:
      type: object
      required: [siteName, allowRegistration, allowGuestAccess, defaultSubmissionPublic, notice]
      properties:
        siteName:
          type: string
          maxLength: 64
        allowRegistration:
          type: boolean
        allowGuestAccess:
          type: boolean
        defaultSubmissionPublic:
          type: boolean
        notice:
          type: string
    BackupSettings:
      type: object
      required: [enabled, frequency, keep, time]
      properties:
        enabled:
          type: boolean
        frequency:
          type: string
          enum: [hourly, daily, weekly]
        keep:
          type: integer
          minimum: 1
          maximum: 100
        time:
          type: string
          example: "03:00"
    BackupList:
      type: object
      required: [items]
      properties:
        running:
          $ref: "#/components/schemas/BackupRunning"
        items:
          type: array
          items:
            $ref: "#/components/schemas/BackupItem"
    BackupRunning:
      type: object
      required: [name, startedAt, stale]
      properties:
        name:
          type: string
        startedAt:
          type: string
          format: date-time
        stale:
          type: boolean
    BackupItem:
      type: object
      required: [name, database, createdAt, size]
      properties:
        name:
          type: string
        database:
          type: string
        createdAt:
          type: string
          format: date-time
        size:
          type: integer
          format: int64
    AdminUser:
      type: object
      required: [id, name, mail, role, groups]
      properties:
        id:
          type: integer
        name:
          type: string
          maxLength: 32
        mail:
          type: string
          format: email
          maxLength: 256
        role:
          type: string
        groups:
          type: array
          items:
            type: integer
    AdminUserUpdate:
      type: object
      required: [role, groups]
      properties:
        role:
          type: string
        groups:
          type: array
          items:
            type: integer
    AdminUserCreate:
      type: object
      required: [name, mail, password, role, groups]
      properties:
        name:
          type: string
          minLength: 3
          maxLength: 32
        mail:
          type: string
          format: email
          maxLength: 256
        password:
          type: string
          minLength: 8
        role:
          type: string
        groups:
          type: array
          items:
            type: integer
    PasswordReset:
      type: object
      required: [password]
      properties:
        password:
          type: string
    AdminGroup:
      type: object
      required: [id, name, users]
      properties:
        id:
          type: integer
        name:
          type: string
          maxLength: 64
        users:
          type: array
          items:
            type: integer
    AdminGroupUpdate:
      type: object
      required: [name, users]
      properties:
        name:
          type: string
          maxLength: 64
        users:
          type: array
          items:
            type: integer
    AdminLang:
      type: object
      required: [id, name, source, dockerfile]
      properties:
        id:
          type: string
          maxLength: 32
        name:
          type: string
          maxLength: 64
        source:
          type: string
          maxLength: 128
        dockerfile:
          type: string
    AdminLangUpdate:
      type: object
      required: [id, name, source, dockerfile]
      properties:
        id:
          type: string
          maxLength: 32
        name:
          type: string
          maxLength: 64
        source:
          type: string
          maxLength: 128
        dockerfile:
          type: string
    AdminLangCreate:
      $ref: "#/components/schemas/AdminLangUpdate"
    AdminJudger:
      type: object
      required: [id, name, online, connectedAt, activeAt, uptimeSeconds]
      properties:
        id:
          type: integer
        name:
          type: string
          maxLength: 64
        token:
          type: string
        online:
          type: boolean
        connectedAt:
          type: string
          format: date-time
          nullable: true
        activeAt:
          type: string
          format: date-time
          nullable: true
        uptimeSeconds:
          type: integer
    AdminJudgeQueue:
      type: object
      required: [queued, running, done]
      properties:
        queued:
          type: integer
        running:
          type: integer
        done:
          type: integer
    AdminJudgerUpdate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 64
        auth:
          type: string
    AdminJudgerCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 64
    Problem:
      type: object
      required:
        - id
        - title
        - tags
        - visible
        - mode
        - timeMs
        - memoryMb
        - cases
        - dataBytes
        - ac
        - submit
        - discussions
        - mine
      properties:
        id:
          type: integer
        sort:
          type: string
          maxLength: 16
        title:
          type: string
          maxLength: 256
        statement:
          type: string
        tags:
          type: array
          items:
            type: string
        visible:
          type: boolean
        mode:
          type: string
          maxLength: 16
        timeMs:
          type: integer
        memoryMb:
          type: integer
        cases:
          type: integer
        dataBytes:
          type: integer
          format: int64
        ac:
          type: integer
        submit:
          type: integer
        discussions:
          type: integer
        mine:
          type: string
          enum: [none, tried, ac]
        latest:
          $ref: "#/components/schemas/ProblemRecord"
    ProblemRecord:
      type: object
      required: [id, status, score, createdAt]
      properties:
        id:
          type: integer
        status:
          type: string
          maxLength: 32
        score:
          type: integer
        createdAt:
          type: string
          format: date-time
    ProblemCreate:
      type: object
      required: [title, tags, visible, mode, timeMs, memoryMb]
      properties:
        title:
          type: string
          maxLength: 256
        tags:
          type: array
          items:
            type: string
        visible:
          type: boolean
        mode:
          type: string
          maxLength: 16
        timeMs:
          type: integer
        memoryMb:
          type: integer
    ProblemUpdate:
      type: object
      required: [title, statement, tags, visible, mode, timeMs, memoryMb]
      properties:
        title:
          type: string
          maxLength: 256
        statement:
          type: string
        tags:
          type: array
          items:
            type: string
        visible:
          type: boolean
        mode:
          type: string
          maxLength: 16
        timeMs:
          type: integer
        memoryMb:
          type: integer
    ProblemVisibilityUpdate:
      type: object
      required: [visible]
      properties:
        visible:
          type: boolean
    AssetFile:
      type: object
      required: [key, name, size, editable]
      properties:
        key:
          type: string
        name:
          type: string
        size:
          type: integer
          format: int64
        editable:
          type: boolean
    ProblemAssets:
      type: object
      required: [data, judge, assets, cases, dataBytes]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/AssetFile"
        judge:
          type: array
          items:
            $ref: "#/components/schemas/AssetFile"
        assets:
          type: array
          items:
            $ref: "#/components/schemas/AssetFile"
        cases:
          type: integer
        dataBytes:
          type: integer
          format: int64
    AssetCaseCreate:
      type: object
      required: [name, input, output]
      properties:
        name:
          type: string
        input:
          type: string
        output:
          type: string
    AssetContent:
      type: object
      required: [key, name, content]
      properties:
        key:
          type: string
        name:
          type: string
        content:
          type: string
    AssetContentUpdate:
      type: object
      required: [key, content]
      properties:
        key:
          type: string
        content:
          type: string
    Home:
      type: object
      required: [notice, heatmap, problems, assignments, contests]
      properties:
        notice:
          type: string
        heatmap:
          type: array
          items:
            $ref: "#/components/schemas/HeatCell"
        problems:
          type: array
          items:
            $ref: "#/components/schemas/Problem"
        assignments:
          type: array
          items:
            $ref: "#/components/schemas/Item"
        contests:
          type: array
          items:
            $ref: "#/components/schemas/Item"
    NoticeUpdate:
      type: object
      required: [content]
      properties:
        content:
          type: string
    Assignment:
      type: object
      required: [id, title, endAt, status, total, done, users, groups]
      properties:
        id:
          type: integer
        title:
          type: string
          maxLength: 256
        endAt:
          type: string
          format: date-time
        status:
          type: string
        total:
          type: integer
        done:
          type: integer
        users:
          type: array
          items:
            type: integer
        groups:
          type: array
          items:
            type: integer
    AssignmentDetail:
      type: object
      required: [assignment, problems, submissions, progress]
      properties:
        assignment:
          $ref: "#/components/schemas/Assignment"
        problems:
          type: array
          items:
            $ref: "#/components/schemas/Problem"
        submissions:
          type: array
          items:
            $ref: "#/components/schemas/Submission"
        progress:
          type: array
          items:
            $ref: "#/components/schemas/AssignmentProgress"
    AssignmentProgress:
      type: object
      required: [user, ac, submit, problems]
      properties:
        user:
          type: string
          maxLength: 32
        ac:
          type: integer
        submit:
          type: integer
        problems:
          type: array
          items:
            $ref: "#/components/schemas/AssignmentProblemProgress"
    AssignmentProblemProgress:
      type: object
      required: [problemId, status]
      properties:
        problemId:
          type: integer
        status:
          type: string
          enum: [none, tried, ac]
    AssignmentCreate:
      type: object
      required: [title, endAt, problems, users, groups]
      properties:
        title:
          type: string
          maxLength: 256
        endAt:
          type: string
          format: date-time
        problems:
          type: array
          items:
            $ref: "#/components/schemas/ProblemRef"
        users:
          type: array
          items:
            type: integer
        groups:
          type: array
          items:
            type: integer
    AssignmentUpdate:
      type: object
      required: [title, endAt, problems, users, groups]
      properties:
        title:
          type: string
          maxLength: 256
        endAt:
          type: string
          format: date-time
        problems:
          type: array
          items:
            $ref: "#/components/schemas/ProblemRef"
        users:
          type: array
          items:
            type: integer
        groups:
          type: array
          items:
            type: integer
    Contest:
      type: object
      required: [id, title, kind, startAt, endAt, freezeAt, status, total]
      properties:
        id:
          type: integer
        title:
          type: string
          maxLength: 256
        kind:
          type: string
          maxLength: 8
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        freezeAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
        total:
          type: integer
    ContestDetail:
      type: object
      required: [contest, problems, rank, submissions]
      properties:
        contest:
          $ref: "#/components/schemas/Contest"
        problems:
          type: array
          items:
            $ref: "#/components/schemas/Problem"
        rank:
          type: array
          items:
            $ref: "#/components/schemas/RankUser"
        submissions:
          type: array
          items:
            $ref: "#/components/schemas/Submission"
    ContestCreate:
      type: object
      required: [title, kind, startAt, endAt, freezeAt, problems]
      properties:
        title:
          type: string
          maxLength: 256
        kind:
          type: string
          maxLength: 8
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        freezeAt:
          type: string
        problems:
          type: array
          items:
            $ref: "#/components/schemas/ProblemRef"
    ContestUpdate:
      type: object
      required: [title, kind, startAt, endAt, freezeAt, problems]
      properties:
        title:
          type: string
          maxLength: 256
        kind:
          type: string
          maxLength: 8
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        freezeAt:
          type: string
        problems:
          type: array
          items:
            $ref: "#/components/schemas/ProblemRef"
    ProblemRef:
      type: object
      required: [id, sort]
      properties:
        id:
          type: integer
        sort:
          type: string
          maxLength: 16
    Submission:
      type: object
      required: [id, problemId, problemTitle, user, language, status, score, public, createdAt]
      properties:
        id:
          type: integer
        problemId:
          type: integer
        problemTitle:
          type: string
          maxLength: 256
        user:
          type: string
          maxLength: 32
        language:
          type: string
          maxLength: 32
        status:
          type: string
          maxLength: 32
        score:
          type: integer
        timeMs:
          type: integer
        memoryKb:
          type: integer
        public:
          type: boolean
        createdAt:
          type: string
          format: date-time
    SubmissionDetail:
      type: object
      required: [submission, code, cases]
      properties:
        submission:
          $ref: "#/components/schemas/Submission"
        code:
          type: string
        cases:
          type: array
          items:
            $ref: "#/components/schemas/Case"
    SubmitRequest:
      type: object
      required: [problemId, language, code, public]
      properties:
        problemId:
          type: integer
        language:
          type: string
          maxLength: 32
        code:
          type: string
        public:
          type: boolean
    SubmissionUpdate:
      type: object
      required: [public]
      properties:
        public:
          type: boolean
    Case:
      type: object
      required: [no, status, message]
      properties:
        no:
          type: integer
        status:
          type: string
          maxLength: 32
        timeMs:
          type: integer
        memoryKb:
          type: integer
        message:
          type: string
          maxLength: 1024
    RankProblem:
      type: object
      required: [problemId, status, submit, score, penalty]
      properties:
        problemId:
          type: integer
        status:
          type: string
          enum: [none, tried, ac]
        submit:
          type: integer
        score:
          type: integer
        penalty:
          type: integer
    RankUser:
      type: object
      required: [rank, user, bio, avatar, ac, submit, score, penalty, problems]
      properties:
        rank:
          type: integer
        user:
          type: string
          maxLength: 32
        bio:
          type: string
          maxLength: 256
        avatar:
          type: string
          maxLength: 512
        ac:
          type: integer
        submit:
          type: integer
        score:
          type: integer
        penalty:
          type: integer
        problems:
          type: array
          items:
            $ref: "#/components/schemas/RankProblem"
    UserProfile:
      type: object
      required: [user, heatmap, solved, activities]
      properties:
        user:
          $ref: "#/components/schemas/PublicUser"
        heatmap:
          type: array
          items:
            $ref: "#/components/schemas/HeatCell"
        solved:
          type: array
          items:
            $ref: "#/components/schemas/Problem"
        activities:
          type: array
          items:
            $ref: "#/components/schemas/UserActivity"
    UserActivity:
      type: object
      required: [type, id, title, createdAt]
      properties:
        type:
          type: string
          enum: [submission, discussion]
        id:
          type: integer
        title:
          type: string
          maxLength: 256
        status:
          type: string
          maxLength: 32
        problemId:
          type: integer
        problemTitle:
          type: string
          maxLength: 256
        createdAt:
          type: string
          format: date-time
    PublicUser:
      type: object
      required: [name, bio, avatar, admin, ac, submit]
      properties:
        name:
          type: string
          maxLength: 32
        bio:
          type: string
          maxLength: 256
        avatar:
          type: string
          maxLength: 512
        admin:
          type: boolean
        ac:
          type: integer
        submit:
          type: integer
    Discussion:
      type: object
      required: [id, title, author, tags, pinned, locked, replies, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
          maxLength: 256
        author:
          type: string
          maxLength: 32
        tags:
          type: array
          items:
            type: string
        pinned:
          type: boolean
        locked:
          type: boolean
        replies:
          type: integer
        createdAt:
          type: string
          format: date-time
    DiscussionCreate:
      type: object
      required: [title, content, tags]
      properties:
        title:
          type: string
          maxLength: 256
        content:
          type: string
        tags:
          type: array
          items:
            type: string
    DiscussionUpdate:
      type: object
      required: [title, content, tags, pinned, locked]
      properties:
        title:
          type: string
          maxLength: 256
        content:
          type: string
        tags:
          type: array
          items:
            type: string
        pinned:
          type: boolean
        locked:
          type: boolean
    DiscussionDetail:
      type: object
      required: [discussion, content, comments]
      properties:
        discussion:
          $ref: "#/components/schemas/Discussion"
        content:
          type: string
        comments:
          type: array
          items:
            $ref: "#/components/schemas/Comment"
    Comment:
      type: object
      required: [id, author, content, createdAt]
      properties:
        id:
          type: integer
        author:
          type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
    CommentCreate:
      type: object
      required: [content]
      properties:
        content:
          type: string
