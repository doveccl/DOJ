# DOJ AI 规则

本文件是当前分支的根级入口规则。保持简短、当前、可执行。不要在这里保存长篇 done list；临时历史记录放到已忽略的 `docs/archive/` 本地目录。

## 优先级

- 优先遵守用户最新的明确指令。
- 面向人的项目使用说明只维护根目录 `README.md`。
- 产品、API、评测、schema、UI 的长期约束按代码目录下就近 `AGENTS.md` 维护。
- 模块级 `AGENTS.md` 只约束所在目录及子目录；跨模块冲突时先遵守用户最新指令，再遵守更深层的 `AGENTS.md`。
- `docs/` 只保留轻量索引或临时设计入口，不再维护会和模块规则重复的大型 PRD 副本。
- 归档文档只作为历史上下文，除非用户明确要求，否则不要纳入 git 追踪。

## 当前硬性方向

- 当前 `main` 分支仍是正式发布前的早期重写分支，可以自由重置数据库迁移历史。
- 代码文件名尽量不要使用连字符 `-`；新文件优先使用 camelCase 或简洁单词命名。
- 页面验收只能使用 IDE 内置浏览器工具；禁止使用 Chrome、CDP、外部浏览器或自写浏览器驱动替代。
- 删除文件或目录只能通过 `rimraf` 命令执行；禁止使用 IDE 删除工具或其他删除工具。
- 委派子代理时必须在任务说明中显式重复上述浏览器和删除约束；无法约束子代理时不得委派相关任务。

## 运行偏好

- `README.md` 只描述项目、开发和部署，英文在前、中文在后。
- 当用户要求按里程碑推进时，每完成一个里程碑或连贯子里程碑后再 push。
- 保持根 `AGENTS.md` 当前有效，不存过时计划、done list、历史审计记录或 docs 摘要副本。
